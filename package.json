{
  "name": "saga-promise-wrapper",
  "version": "1.0.0",
  "description": "A function to wrap a promise to make it look like a saga for redux-saga",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Karl von Randow",
  "license": "Apache-2.0",
  "devDependencies": {
    "redux": "^4.0.5",
    "redux-saga": "^1.1.3",
    "typescript": "^4.0.5"
  },
  "peerDependencies": {
    "redux": "^4.0.5",
    "redux-saga": "^1.1.3"
  }
}
